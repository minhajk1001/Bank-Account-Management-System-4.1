#include <iostream>
#include <sstream>
#include <stdio.h>


using namespace std;

int main(){

    int tInt,nmInt,mdls;

    scanf("%d",&tInt);

    for (int i=1; i<=tInt; i++)
    {
        scanf("%d",&nmInt);
        mdls=nmInt%10;
        for(int j=0,k=90,l=10;j<=9; j++,k-=9,l--){
            if((mdls==j)&&(nmInt>=k))
                printf("%d\n",l);
            if((mdls==j)&&(nmInt<k))
                printf("Impossible\n",l);
        }

    }

} 
